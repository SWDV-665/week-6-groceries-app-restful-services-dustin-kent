import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'groceries.server',
  appName: 'groceries-server',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
